---
id: LT-008.04
title: Match extraction wizard UI (extract -> resolve -> review/confirm)
status: Done
assignee: []
created_date: '2026-07-25 13:32'
updated_date: '2026-07-27 20:55'
labels:
  - htmx
  - frontend
dependencies:
  - LT-008.03
parent_task_id: LT-008
priority: medium
ordinal: 28000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Three-step Qute + HTMX wizard mirroring ParticipationImportResource/step{1,2,3}.html, applied to matches: step 1 picks competition (or all) + date; step 2 shows discovered matches with per-side team resolution and a resolved-vs-ambiguous player breakdown per match; step 3 is the final confirm/save (plain form POST + 303 redirect, matching the existing wizard's save-step pattern, since ambiguous rows go to the inbox rather than blocking). Admin-only (@RolesAllowed("ADMIN")), linked from templates/tags/appNav.html.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Admin can pick a competition (or all competitions) + date and see discovered matches with per-match resolved/ambiguous player counts before confirming
- [x] #2 Confirming persists the matches and redirects (303) to a sensible landing page (e.g. the match list)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implemented MatchExtractionResource (extraction package) as a 3-step Qute+HTMX wizard mirroring ParticipationImportResource: step1 picks competition+fixturesUrl+season+date; discover calls MatchExtractionService.preview() once and renders step2 with per-match home/away resolution + resolved/ambiguous player counts; review reconstructs rows from hidden fields (no re-scrape) and renders step3; confirm calls MatchExtractionService.confirm() and 303-redirects to /matches. Wired 'Извлечи за дата...' nav link. Added MatchExtractionResourceTest (13 tests, mocking MatchExtractionService). Left 'Извлечи за днес' and 'Планировчик' nav stubs unwired -- out of scope (today-shortcut and LT-009 scheduler UI).
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Tests are added for new functoinality and mvn verify is successfull
<!-- DOD:END -->
