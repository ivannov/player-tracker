---
id: LT-001.03
title: Import wizard UI — Qute templates and HTMX wiring
status: Done
assignee: []
created_date: '2026-05-03 19:45'
updated_date: '2026-07-12 08:03'
labels:
  - frontend
  - htmx
  - qute
dependencies:
  - LT-001.02
parent_task_id: LT-001
priority: high
ordinal: 3000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Build the Qute templates and HTMX interactions for the three-step participation import wizard (LT-001). Backend endpoints provided by LT-001.02.

**Step 1 — `templates/ParticipationImportResource/step1.html`**
Full page (extends layout). Inputs: URL text field, competition `<select>` (from DB), season text field (placeholder `2024/2025`). Submit: `hx-post="/participations/import/extract"` → swaps `#wizard-body`.

**Step 2 — `templates/ParticipationImportResource/step2.html`** (HTMX partial, no layout)
One row per scraped team:
- Scraped name as read-only label
- Team picker: `<input list="teams-list">` + `<datalist id="teams-list">` with all DB teams (value=id, label=name) — native browser search, no JS library needed. Hidden `teamId` field synced via small inline script on input change.
- "New team" toggle: reveals name + location inputs, hides the datalist picker
- Formation picker: `<select>` of FormationTypes; on team selection fires `hx-post="/participations/import/formations"` to swap in only that team's existing formations + "add new formation" option
- "Review" button posts full form to `#wizard-body` → step 3

**Step 3 — `templates/ParticipationImportResource/step3.html`** (HTMX partial, no layout)
Summary table: scraped name | resolved team | formation | action (create / already exists / skip). Hidden inputs forwarded from step 2. Confirm button: `hx-post="/participations/import/save"` with full-page redirect on 303.

**Navigation:** add "Import from BFU" button/link to existing `ParticipationResource/list.html`.

**CLAUDE.md conventions to observe:**
- Shared nav: `{#appNav username=username /}`
- `referrerpolicy="no-referrer"` on any BFU `<img>` tags
- No `{N}` quantifiers in HTML attributes (Qute parses `{4}` as template expression)
- Pico CSS classless — no additional CSS framework
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Step 1 page renders at GET /participations/import with competition dropdown populated from DB
- [x] #2 Submitting step 1 replaces #wizard-body with step 2 — no full-page reload
- [x] #3 Pre-matched rows in step 2 have the correct team name pre-filled in the datalist input
- [x] #4 Changing the team picker fires HTMX and swaps the formation selector with that team's formations
- [x] #5 Toggling 'create new team' reveals name/location inputs and hides the datalist picker
- [x] #6 Step 2 Review replaces #wizard-body with step 3 summary
- [x] #7 Step 3 Confirm saves and redirects to /participations list
- [x] #8 'Import from BFU' link is visible on the participations list page
- [x] #9 Page is usable on a standard desktop viewport with Pico CSS — no layout breakage
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Reused existing LT-001.02 backend (extract/formations/save) as-is. Added a new read-only POST /participations/import/review endpoint that mirrors save()'s exact branching (blank row / new team / existing team+given formation / existing team+new formation type / no formation chosen) but computes display labels + a CREATE/EXISTS/SKIP classification instead of persisting, then renders step3.html. Folded in a security-convention fix while touching the file: GET step1 moved from bare @Authenticated to @RolesAllowed("ADMIN"), and raw SecurityIdentity swapped for the shared CurrentUser bean (matching the ParticipationResource convention established by the LT-002 cleanup). Rewrote step2.html: one shared <datalist> of all teams (value="name (location)" + data-id, not value=id, since browsers match/display datalist suggestions by value not label), a per-row toggle between a searchable team input and free-text new-team fields, and an inline <script> that resolves the typed value to a team id and dispatches a change event on a hidden teamId field to drive the existing /formations htmx swap. formationsSelect.html gained a second "add new formation" affordance for existing teams (sentinel option + hidden newFormationType select, revealed via script). step2's submit now posts to /review (was /save) so users see a create/exists/skip preview before committing.
<!-- SECTION:PLAN:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Implemented the step2 rewrite (datalist team search + new-team toggle + add-new-formation affordance) and a new step3 review/confirm page per the task spec, plus the CurrentUser/@RolesAllowed("ADMIN") security alignment agreed with the user (the original task predated LT-002's security-model convention). New files: ImportAction.java, ImportReviewRow.java, step3.html. Added 8 new tests (GET/review admin-gating, CREATE/EXISTS/SKIP classification, no-side-effects dry-run guarantee) — ParticipationImportResourceTest now has 14 tests, full mvn verify green. Manually verified end-to-end against a throwaway local Postgres + dev server via curl (login, /formations fragment, /review classification for all four action types, /save persisting exactly what review predicted, list page + admin gating) since no browser-automation tool was available in this environment to exercise the client-side datalist/toggle JS interactively — the server-rendered HTML/JS was inspected instead.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Tests are added for new functoinality and mvn verify is successfull
<!-- DOD:END -->
