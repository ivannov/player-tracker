---
id: LT-005.03
title: Local AI embedding matching via Ollama
status: To Do
assignee: []
created_date: '2026-07-11 15:43'
labels: []
dependencies:
  - LT-005.02
parent_task_id: LT-005
priority: medium
ordinal: 15000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Layers local AI embeddings on top of the trigram matching from LT-005.02, per the user's explicit choice to include this from the start rather than as a later enhancement. New OllamaEmbeddingClient in the matching package, using the already-present-but-unused quarkus-rest-client dependency in pom.xml. Embedding population runs asynchronously (e.g. on player creation and via a catch-up pass for existing players) so it never sits on the request path of an admin action. The fallback-to-trigram behavior when Ollama is down is a hard requirement -- this must never become a single point of failure for basic name resolution.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 docker-compose.yml gains an ollama service; the chosen embedding model (e.g. nomic-embed-text) is documented in README.md including the pull command
- [ ] #2 OllamaEmbeddingClient (quarkus-rest-client, already an unused pom dependency) computes an embedding vector for a given player name
- [ ] #3 A background mechanism populates name_embedding for players missing it, without blocking request threads
- [ ] #4 PlayerMatchingService re-ranks trigram candidates using cosine distance against name_embedding when available
- [ ] #5 If Ollama is unreachable, PlayerMatchingService falls back gracefully to trigram-only matching rather than failing
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Tests are added for new functoinality and mvn verify is successfull
<!-- DOD:END -->
