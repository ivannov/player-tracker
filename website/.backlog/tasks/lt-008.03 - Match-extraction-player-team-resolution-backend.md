---
id: LT-008.03
title: Match extraction + player/team resolution backend
status: To Do
assignee: []
created_date: '2026-07-25 13:32'
labels:
  - scraping
  - backend
dependencies:
  - LT-005
parent_task_id: LT-008
priority: medium
ordinal: 27000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Backend service (reusable by LT-009's scheduled job later) that, given a competition (or all) + date: runs fixture discovery, for each match tries the bfu-tournaments.com adapter first and falls back to the ebfu.net adapter (lineups only) when the primary has nothing, resolves each side's team via the shared team-resolution service, resolves every scraped player name via PlayerMatchingService.resolve(rawName, teamId, source) -- confidently-resolved rows collected for admin confirmation, ambiguous rows written to AmbiguityReview with match populated (see LT-010's Participation-based side derivation) -- and on confirm persists Match/PlayerAppearance/MatchEvent rows, guarding against duplicate re-import via the existing (player_id, match_id) unique constraint.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Given a competition+date, produces a preview of resolved/ambiguous rows without persisting anything (dry-run), matching the existing wizard's review-step pattern
- [ ] #2 Confirm step persists Match, PlayerAppearance (with correct participation/side per LT-010), and MatchEvent rows; re-running the same match/date is safe (no duplicate rows) via the (player_id, match_id) constraint
- [ ] #3 Ambiguous player names are queued to AmbiguityReview with match populated instead of blocking the save
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Tests are added for new functoinality and mvn verify is successfull
<!-- DOD:END -->
