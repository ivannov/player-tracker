---
id: LT-008.01
title: bfu-tournaments.com match-day fixture discovery
status: Done
assignee: []
created_date: '2026-07-25 13:32'
updated_date: '2026-07-25 13:43'
labels:
  - scraping
  - backend
dependencies:
  - LT-007.01
parent_task_id: LT-008
priority: medium
ordinal: 25000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Given a competition + date (or 'all competitions' + date), discover which matches were played/scheduled that day and return each match's bfu-tournaments.com page URL plus the two team names (whatever else the listing page cheaply exposes, e.g. status/score preview). This is the missing piece the LT-008 stub flags: BfuMatchScraperService (LT-007.01) only scrapes an already-known match URL, and BfuLeagueScraperService (LT-001.01) only lists a league's teams, not a schedule -- neither can answer 'what happened on date X'. Mirror the existing Jsoup + checked-exception + fixture-based-test pattern (BfuLeagueScraperService / BfuMatchScraperService). Needs real recon against bfu-tournaments.com to find the actual competition+date listing page structure before writing the parser (see experiences/bfu-scraping for the recon-workaround pattern used for LT-007.01/LT-006.01).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Given a competition and a date, returns the list of match URLs (+ team names) for that day; returns an empty list when nothing was played
- [ ] #2 Throws the existing checked scraper exception (or a same-shaped new one) on structural page surprises rather than silently returning nulls/partial data
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Recon confirmed the league page's ?view=past-matches Livewire tab statically renders every round of the season in one page load (accordion clicks are client-side only, no network request). BfuFixtureScraperService.findMatches(url, date) parses <tr> rows (date dd + home dd + result <a href=stats/match/{id}> + away dd), matching on java.time.MonthDay since the site's date text has no year. Fixture is a trimmed-but-real capture (rounds 35+36) of the first-league 2024-2025 results page.
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Tests are added for new functoinality and mvn verify is successfull
<!-- DOD:END -->
